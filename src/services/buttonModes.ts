import { Modes } from '../domain/song';

export const buttonModes: Modes[] = ['Not replaying', 'Replaying all', 'Replaying one'];

export const enum ModesValues {
    NOT_REPLAYING = 'Not replaying',
    REPLAYING_ALL = 'Replaying all',
    REPLAYING_ONE = 'Replaying one',
}
